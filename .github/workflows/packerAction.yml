name: Packer Continuous Integration Actions to Build custom image

on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - main

jobs:
  packer_template_validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Packer
        uses: hashicorp/setup-packer@main

      - name: Create Zip File of Webapp Repo
        run: |
          zip -r webapp.zip .

      - name: Move Zip to Workflows Directory
        run: mv webapp.zip vm-image/

      - name: Format Packer Configs Check
        run: |
          cd vm-image/
          if packer fmt -check -diff gcpBuild.pkr.hcl; then
            echo "No formatting changes detected"
            echo "::set-output name=changed::false"
          else
            echo "Formatting changes detected"
            echo "::set-output name=changed::true"
            exit 1
          fi

      - name: Init and Validate Packer Configs
        run: |
          cd vm-image/
          packer init gcpBuild.pkr.hcl
          packer validate gcpBuild.pkr.hcl
